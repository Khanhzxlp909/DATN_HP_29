// ProductCard.vue
<template>
  <div class="card">
    <div>
      <img :src="product.image" class="card-img-top" :alt="product.name" />
      <form class="hover-icon hidden-sm hidden-xs">
        <a href="/pay" class="btn-add-to-cart" title="Mua ngay">
          <i class="fas fa-cart-plus"></i>
        </a>
        <a class="quickview" @click="$emit('view-detail', product.id)" title="Xem nhanh">
          <i class="fas fa-search"></i>
        </a>
      </form>
    </div>
    <a @click="$emit('view-detail', product.id)">
      <div class="card-body">
        <h5 class="card-title">{{ product.name }}</h5>
        <h5 class="card-title">Cân nặng: {{ product.weight }}</h5>
        <h5 class="card-title">Vật liệu: {{ product.material }}</h5>
        <div class="product__price">
          <p class="card-text">{{ product.price }} VND</p>
        </div>
      </div>
    </a>
  </div>
</template>

<script>
export default {
  props: ["product"],
};
</script>
